pipeline{

    agent any

    stages{

        stage("build"){
            steps{
                echo 'building the applications'
            }
        }

        steps("test"){
            echo 'testing the aplications validate'
        }

        steps("deploy"){
            echo 'deploying the applications'
        }

    }
}